steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['beta', 'functions', 'deploy', '${_FUNCTION_NAME}',
   '--allow-unauthenticated',
   '--project', '${_GCP_PROJECT}',
   '--region', '${_REGION}',
   '--service-account','${_SERVICE_ACCOUNT}',
   '--runtime', 'nodejs10',
   '--trigger-topic', '${_TRIGGER_TOPIC}',
   '--entry-point', 'commandSub',
   '--quiet']
